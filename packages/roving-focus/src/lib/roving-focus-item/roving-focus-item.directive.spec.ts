import { RovingFocusItemDirective } from './roving-focus-item.directive';

describe('RovingFocusItemDirective', () => {
  it('should create an instance', () => {
    const directive = new RovingFocusItemDirective();
    expect(directive).toBeTruthy();
  });
});
