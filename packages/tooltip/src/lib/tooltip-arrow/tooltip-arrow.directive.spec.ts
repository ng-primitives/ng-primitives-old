import { TooltipArrowDirective } from './tooltip-arrow.directive';

describe('TooltipArrowDirective', () => {
  it('should create an instance', () => {
    const directive = new TooltipArrowDirective();
    expect(directive).toBeTruthy();
  });
});
