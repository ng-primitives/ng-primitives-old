import { RovingFocusDirective } from './roving-focus.directive';

describe('RovingFocusDirective', () => {
  it('should create an instance', () => {
    const directive = new RovingFocusDirective();
    expect(directive).toBeTruthy();
  });
});
