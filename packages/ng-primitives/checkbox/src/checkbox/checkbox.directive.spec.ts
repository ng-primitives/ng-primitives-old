import { NgpCheckboxDirective } from './checkbox.directive';

describe('NgpCheckboxDirective', () => {
  it('should create an instance', () => {
    const directive = new NgpCheckboxDirective();
    expect(directive).toBeTruthy();
  });
});
