import { NgpCheckboxIndicatorDirective } from './checkbox-indicator.directive';

describe('NgpCheckboxIndicatorDirective', () => {
  it('should create an instance', () => {
    const directive = new NgpCheckboxIndicatorDirective();
    expect(directive).toBeTruthy();
  });
});
