import { NgpSelectOptionsDirective } from './select-options.directive';

describe('NgpSelectOptionsDirective', () => {
  it('should create an instance', () => {
    const directive = new NgpSelectOptionsDirective();
    expect(directive).toBeTruthy();
  });
});
