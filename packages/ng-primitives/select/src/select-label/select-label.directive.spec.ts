import { NgpSelectLabelDirective } from './select-label.directive';

describe('NgpSelectValueDirective', () => {
  it('should create an instance', () => {
    const directive = new NgpSelectLabelDirective();
    expect(directive).toBeTruthy();
  });
});
