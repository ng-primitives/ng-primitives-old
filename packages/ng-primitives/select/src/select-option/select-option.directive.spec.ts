import { NgpSelectOptionDirective } from './select-option.directive';

describe('NgpSelectOptionDirective', () => {
  it('should create an instance', () => {
    const directive = new NgpSelectOptionDirective();
    expect(directive).toBeTruthy();
  });
});
