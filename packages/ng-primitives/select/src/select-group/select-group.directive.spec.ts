import { NgpSelectGroupDirective } from './select-group.directive';

describe('NgpSelectGroupDirective', () => {
  it('should create an instance', () => {
    const directive = new NgpSelectGroupDirective();
    expect(directive).toBeTruthy();
  });
});
