import { NgpSelectTriggerDirective } from './select-trigger.directive';

describe('NgpSelectTriggerDirective', () => {
  it('should create an instance', () => {
    const directive = new NgpSelectTriggerDirective();
    expect(directive).toBeTruthy();
  });
});
