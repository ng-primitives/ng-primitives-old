import { AccordionPanelDirective } from './accordion-panel.directive';

describe('AccordionPanelDirective', () => {
  it('should create an instance', () => {
    const directive = new AccordionPanelDirective();
    expect(directive).toBeTruthy();
  });
});
