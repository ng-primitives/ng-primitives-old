import { NgpToggleGroupDirective } from './toggle-group.directive';

describe('NgpToggleGroupDirective', () => {
  it('should create an instance', () => {
    const directive = new NgpToggleGroupDirective();
    expect(directive).toBeTruthy();
  });
});
