import { NgpToggleGroupButtonDirective } from './toggle-group-button.directive';

describe('NgpToggleGroupButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new NgpToggleGroupButtonDirective();
    expect(directive).toBeTruthy();
  });
});
