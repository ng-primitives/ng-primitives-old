import { NgpTabPanelDirective } from './tab-panel.directive';

describe('NgpTabPanelDirective', () => {
  it('should create an instance', () => {
    const directive = new NgpTabPanelDirective();
    expect(directive).toBeTruthy();
  });
});
