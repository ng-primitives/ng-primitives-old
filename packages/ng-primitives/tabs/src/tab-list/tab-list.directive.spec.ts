import { TabListDirective } from './tab-list.directive';

describe('TabListDirective', () => {
  it('should create an instance', () => {
    const directive = new TabListDirective();
    expect(directive).toBeTruthy();
  });
});
