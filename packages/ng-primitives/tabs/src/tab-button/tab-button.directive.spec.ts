import { TabButtonDirective } from './tab-button.directive';

describe('TabButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new TabButtonDirective();
    expect(directive).toBeTruthy();
  });
});
