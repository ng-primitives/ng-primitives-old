import { NgpTabsetDirective } from './tabset.directive';

describe('NgpTabsetDirective', () => {
  it('should create an instance', () => {
    const directive = new NgpTabsetDirective();
    expect(directive).toBeTruthy();
  });
});
