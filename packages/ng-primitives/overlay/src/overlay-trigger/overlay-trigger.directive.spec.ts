import { NgpOverlayTriggerDirective } from './overlay-trigger.directive';

describe('OverlayTriggerDirective', () => {
  it('should create an instance', () => {
    const directive = new NgpOverlayTriggerDirective();
    expect(directive).toBeTruthy();
  });
});
