import { NgpSwitchDirective } from './switch.directive';

describe('NgpSwitchDirective', () => {
  it('should create an instance', () => {
    const directive = new NgpSwitchDirective();
    expect(directive).toBeTruthy();
  });
});
