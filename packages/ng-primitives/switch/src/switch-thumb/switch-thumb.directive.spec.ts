import { NgpSwitchThumbDirective } from './switch-thumb.directive';

describe('NgpSwitchThumbDirective', () => {
  it('should create an instance', () => {
    const directive = new NgpSwitchThumbDirective();
    expect(directive).toBeTruthy();
  });
});
