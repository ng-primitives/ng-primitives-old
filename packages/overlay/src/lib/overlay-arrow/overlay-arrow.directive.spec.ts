import { NgpOverlayArrowDirective } from './overlay-arrow.directive';

describe('OverlayArrowDirective', () => {
  it('should create an instance', () => {
    const directive = new NgpOverlayArrowDirective();
    expect(directive).toBeTruthy();
  });
});
