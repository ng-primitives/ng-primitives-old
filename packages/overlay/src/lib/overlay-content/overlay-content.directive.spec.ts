import { NgpOverlayContentDirective } from './overlay-content.directive';

describe('OverlayContentDirective', () => {
  it('should create an instance', () => {
    const directive = new NgpOverlayContentDirective();
    expect(directive).toBeTruthy();
  });
});
